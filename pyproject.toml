[project]
name = "diagramatic-dialogues"
version = "0.1.0"
description = "Read a codebase as a sketch, and interrogate it by planting flags on the picture."
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.30",
    "jinja2>=3.1",
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "httpx>=0.27"]

[tool.pytest.ini_options]
testpaths = ["tests"]
